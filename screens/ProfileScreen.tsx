import React from "react";
import { YStack, Text } from "tamagui";

const ProfileScreen = () => {
  return (
    <YStack px={25} borderWidth={1} flex={1} borderColor={"green"}>
      <Text>ProfileScreen</Text>
    </YStack>
  );
};

export default ProfileScreen;
